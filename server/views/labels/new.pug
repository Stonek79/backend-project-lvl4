extends ../layouts/application.pug
block header
  = t('views.statuses.new.create')

block content
  +formFor()(action=route('labels') method='post')
    +input(label, 'name', t('views.labels.new.name'), errors)(type="text", autofocus)
    +submit()(value=t('views.labels.new.submit'))
    